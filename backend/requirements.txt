# Web Framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.13.0

# Cache & Message Queue (可选，无 Redis 时降级为内存)
redis>=5.0.0
celery>=5.4.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# HTTP Client for InfiniSynapse
aiohttp>=3.9.0
aiofiles>=24.1.0

# Storage (可选，无 MinIO 时降级为本地文件)
minio>=7.2.0

# Data Processing
openpyxl>=3.1.0
pyyaml>=6.0

# PDF Generation (reportlab 有预编译 wheel)
reportlab>=4.0.0
qrcode>=7.4.0
Pillow>=10.0.0

# Validation & Settings
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0
ulid-py>=1.1.0
orjson>=3.10.0
